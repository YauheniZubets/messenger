import PagesLinks from '../pages/PagesLinks';

import './start.css';

const Start = (props) => {

    return (
        <div className='pages-ul'>
            <PagesLinks />
        </div>
    )
}

export default Start;